import * as UI from "../UI/UI";
export declare namespace Components {
    class App<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Main<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Nav<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Article<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Section<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Aside<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Text<T> extends UI.ElementUI<T> {
        constructor(arg: any, position?: string);
    }
    class H1<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Dialog<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Div<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Span<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Container<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Form<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Fieldset<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Button<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Input<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class DataList<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Output<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Label<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Textarea<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Select<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Option<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class OptionGroup<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class HTMLObject<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Iframe<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Canvas<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Svg<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Path<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Link<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Ul<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Li<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
    class Image<T> extends UI.ElementUI<T> {
        constructor(arg: UI.TemplateInterface<T>);
    }
}
